/* Changelog 
=================================================================
DATE:		Author		Function			Changes
DD-MMM-YYYY										

=================================================================
/**/

void main()
{
	object oPC = GetPCSpeaker();
	SendMessageToPC(oPC, "No more refund given for chests");
}